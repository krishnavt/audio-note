{
  "name": "audionote",
  "version": "1.0.0",
  "description": "AI-powered speech-to-text with enhancement capabilities",
  "main": "index.html",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp -r *.html *.css *.js public/",
    "start": "vercel dev"
  },
  "dependencies": {
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  },
  "engines": {
    "node": "20.x"
  }
}